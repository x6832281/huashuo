# 情绪树洞模块 - 详细规格说明

## 1. 模块概述

### 1.1 模块名称
情绪树洞模块（Emotion Tree Hole Module）

### 1.2 模块目标
提供用户心事的发布、浏览、管理功能，支持情绪频段的自动识别和手动纠错。

### 1.3 模块定位
作为应用的核心功能模块，为用户提供情绪表达和管理的平台。

## 2. 功能需求

### 2.1 核心功能
- **心事发布**：用户发布纯文字心事
- **情绪频段识别**：AI自动识别情绪频段
- **频段纠错**：用户可手动修改情绪频段（仅限1次）
- **心事列表**：按时间排序展示心事
- **心事筛选**：按情绪频段、归档状态筛选
- **心事详情**：查看心事详情
- **心事管理**：归档、删除心事
- **脱敏提示**：检测疑似敏感信息并提醒

### 2.2 业务规则
- **内容限制**：心事内容为纯文字，长度限制为1-1000字
- **情绪频段**：仅3档（🌧️=0低落/焦虑/疲惫，🌤️=1平静/波动/复杂，☀️=2轻松/积极/被接住）
- **频段纠错**：用户可在发布后对频段手动修改1次，修改后锁定不可再改
- **脱敏提示**：检测疑似手机号、地址等敏感信息并提醒，但不强拦截
- **数据隔离**：心事数据与用户身份严格绑定，切换身份后只能访问当前身份的心事

## 3. 技术实现

### 3.1 技术栈
- **前端框架**：Vue.js / React
- **存储**：本地存储模块（IndexedDB）
- **状态管理**：Vuex / Redux（可选）
- **AI集成**：后端AI翻译API

### 3.2 实现方案

#### 3.2.1 核心数据结构
```javascript
interface Post {
  id: string;                  // 唯一标识符
  identity_id: string;         // 所属身份ID
  mood_band_ai: number;        // AI建议的情绪频段
  mood_band_final: number;     // 最终情绪频段（用户可纠错）
  mood_band_edit_count: number; // 频段编辑次数（0或1）
  content_text: string;        // 心事内容
  created_at: number;          // 创建时间戳
  archived_at?: number;        // 归档时间戳（可选）
  deleted_at?: number;         // 删除时间戳（可选）
}
```

#### 3.2.2 核心方法

##### 3.2.2.1 发布心事
```javascript
async function createPost(contentText) {
  // 验证内容
  // 检测敏感信息
  // 调用AI获取情绪频段
  // 创建心事对象
  // 存储到本地存储
  // 返回创建的心事
}
```

##### 3.2.2.2 获取心事列表
```javascript
function getPostList(filters) {
  // 从本地存储读取心事
  // 应用筛选条件
  // 按时间排序
  // 返回心事列表
}
```

##### 3.2.2.3 获取心事详情
```javascript
function getPostDetail(postId) {
  // 从本地存储读取心事详情
  // 返回心事详情
}
```

##### 3.2.2.4 编辑情绪频段
```javascript
async function editMoodBand(postId, newMoodBand) {
  // 验证心事ID
  // 检查编辑次数
  // 调用AI重新生成卡片
  // 更新心事数据
  // 存储到本地存储
  // 返回更新后的心事
}
```

##### 3.2.2.5 归档心事
```javascript
function archivePost(postId) {
  // 验证心事ID
  // 更新心事状态为归档
  // 存储到本地存储
}
```

##### 3.2.2.6 删除心事
```javascript
function deletePost(postId) {
  // 验证心事ID
  // 更新心事状态为删除
  // 存储到本地存储
}
```

##### 3.2.2.7 检测敏感信息
```javascript
function detectSensitiveInfo(text) {
  // 检测手机号、地址等敏感信息
  // 返回检测结果
}
```

## 4. 测试方案

### 4.1 单元测试
- **测试场景**：
  - 心事发布成功
  - 情绪频段识别正确
  - 频段纠错功能正常
  - 心事列表获取成功
  - 心事筛选功能正常
  - 心事管理功能正常
  - 敏感信息检测正确
  - 错误处理正确

### 4.2 集成测试
- **测试场景**：
  - 与本地存储模块集成
  - 与身份管理模块集成
  - 与AI翻译模块集成
  - 与卡片生成模块集成

### 4.3 用户测试
- **测试场景**：
  - 发布心事流程
  - 情绪频段纠错流程
  - 心事列表浏览和筛选
  - 心事归档和删除
  - 敏感信息检测提示

## 5. 部署与兼容性

### 5.1 浏览器兼容性
- **支持的浏览器**：
  - Chrome 60+
  - Firefox 55+
  - Safari 12+
  - Edge 79+

### 5.2 响应式设计
- **适配设备**：
  - 移动设备（320px-768px）
  - 平板设备（768px-1024px）
  - 桌面设备（1024px+）

## 6. 风险与应对

### 6.1 风险
- **数据丢失**：用户清除浏览器数据可能导致心事数据丢失
- **AI识别错误**：AI可能对情绪频段识别错误
- **性能问题**：大量心事数据可能影响加载性能

### 6.2 应对措施
- **数据备份**：提供数据导出功能，允许用户备份心事数据
- **人工纠错**：允许用户手动修改情绪频段
- **分页加载**：实现心事列表的分页加载，提高性能

## 7. 依赖关系

### 7.1 依赖模块
- 本地存储模块
- 身份管理模块
- AI翻译模块

### 7.2 被依赖模块
- 卡片生成模块
- 拥抱回流模块

## 8. 开发计划

### 8.1 开发步骤
1. 设计心事数据结构
2. 实现心事发布功能
3. 实现情绪频段识别集成
4. 实现频段纠错功能
5. 实现心事列表和筛选
6. 实现心事详情展示
7. 实现心事管理功能
8. 实现敏感信息检测
9. 编写测试用例
10. 集成测试

### 8.2 预期完成时间
- **估计时间**：2-3周
- **关键路径**：心事发布和情绪频段识别功能实现

## 9. 验收标准

### 9.1 功能验收
- 心事发布成功
- 情绪频段识别正确
- 频段纠错功能正常
- 心事列表展示正确
- 心事筛选功能正常
- 心事管理功能正常
- 敏感信息检测提示正确

### 9.2 性能验收
- 心事发布响应时间 < 2s
- 心事列表加载时间 < 1s
- 操作流畅无卡顿

### 9.3 兼容性验收
- 在支持的浏览器中正常工作
- 在不同设备上表现一致
- 响应式设计适配不同屏幕尺寸